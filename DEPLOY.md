# 🚀 Гайд для будущего разработчика: Деплоим свой проект с Картами Москвы

Привет! В этом гайде я расскажу тебе, как превратить этот код в работающий сайт в интернете. Мы будем использовать профессиональные инструменты: **GitHub**, **Supabase** и **Vercel**.

Следуй шагам внимательно, и у тебя всё получится!

---

## 🛠 Шаг 1: Подготовка кода и GitHub

Чтобы сохранить свой проект и отправить его на сервер, нам нужен **GitHub**.

1.  **Создай аккаунт на [GitHub](https://github.com/)**, если его ещё нет.
2.  **Создай новый репозиторий**:
    - Нажми на `+` в верхнем углу -> `New repository`.
    - Придумай имя (например, `moscow-map`).
    - Выбери **Private** (чтобы твой код видел только ты).
    - **Не** ставь галочки на README или лицензиях.
3.  **Отправь код в GitHub** (в терминале твоего VS Code):
    ```bash
    git init
    git add .
    git commit -m "feat: first commit"
    git branch -M main
    git remote add origin https://github.com/ТВОЁ_ИМЯ/ИМЯ_РЕПОЗИТОРИЯ.git
    git push -u origin main
    ```

---

## 💾 Шаг 2: Настраиваем Базу Данных (Supabase)

Нам нужно место, где будут храниться данные о точках на карте. Мы заменим стандартный SQLite на мощный **PostgreSQL** в Supabase.

1.  Зайди на [Supabase](https://supabase.com/) и создай новый проект.
2.  Придумай пароль для базы данных (запиши его!).
3.  Найди раздел **Project Settings -> Database**.
4.  Тебе нужна строка **Connection String (URI)**. Она выглядит примерно так:
    `postgresql://postgres.xxx:password@xxx.supabase.co:5432/postgres`
    - _Важно:_ Замени `[YOUR-PASSWORD]` на тот пароль, который ты придумал.

---

## 🌍 Шаг 3: Запуск в интернете (Vercel)

Vercel — это место, где твой сайт будет "жить".

1.  Зайди на [Vercel](https://vercel.com/) и авторизуйся через свой GitHub.
2.  Нажми **Add New -> Project**.
3.  Найди свой репозиторий `moscow-map` и нажми **Import**.
4.  **Самое важное: Переменные окружения (Environment Variables)**.
    Раскрой раздел и добавь следующие ключи и значения:

    | Key                           | Value                          | Где взять?                                           |
    | :---------------------------- | :----------------------------- | :--------------------------------------------------- |
    | `DATABASE_URI`                | Твоя строка из Supabase        | См. Шаг 2                                            |
    | `PAYLOAD_SECRET`              | Любая длинная случайная строка | Придумай сам (напр. `super-secret-123`)              |
    | `NEXT_PUBLIC_YANDEX_MAPS_KEY` | Твой ключ API Яндекс Карт      | Если есть, вставь сюда                               |
    | `NEXT_PUBLIC_SERVER_URL`      | Ссылка, которую даст Vercel    | Пока оставь пустой или впиши `http://localhost:3000` |

5.  Нажми **Deploy**! Через пару минут твой сайт будет готов.

---

## 🧩 Шаг 4: Как сменить Базу Данных в коде (для Supabase)

По умолчанию проект настроен на SQLite. Если хочешь использовать Supabase (Postgres) во время разработки:

1.  Установи адаптер для Postgres:
    ```bash
    npm install @payloadcms/db-postgres
    ```
2.  В файле `src/payload.config.ts` поменяй импорт и настройку:
    - Вместо `sqliteAdapter` импортируй `postgresAdapter` из `@payloadcms/db-postgres`.
    - Замени блок `db: sqliteAdapter(...)` на `db: postgresAdapter({ pool: { connectionString: process.env.DATABASE_URI } })`.

---

## 🆘 Если что-то не получается

- **Ошибка базы данных?** Проверь, правильно ли вставлен пароль в `DATABASE_URI`.
- **Сайт не открывается?** Посмотри в Vercel раздел **Logs**, там написано, на какой строчке кода произошла ошибка.
- **Пустая карта?** Убедись, что ты добавил локации в админку (твой-сайт.vercel.app/admin).

**Удачи, будущий разработчик! Ты на верном пути! 🚀**

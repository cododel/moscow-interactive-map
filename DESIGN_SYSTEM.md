# Design System: Moscow Interactive Map

Данная документация описывает дизайн-систему проекта "Интерактивная карта Москвы". Основная концепция дизайна — **Modern Premium** с элементами **Glassmorphism** и **Minimalism**, получившая название **"Moscow Interactive Elegance"**.

---

## 🎨 Цветовая палитра (Color Palette)

Мы используем сдержанную, но глубокую палитру, ориентированную на читаемость и эстетику премиального продукта.

| Название              | Значение                | Описание                                |
| :-------------------- | :---------------------- | :-------------------------------------- |
| **Background**        | `#fafafa`               | Основной фон страниц (Off-white)        |
| **Card Background**   | `#ffffff`               | Чисто белый для интерактивных элементов |
| **Primary (Blue)**    | `#0070f3`               | Главный акцентный цвет (Action color)   |
| **Primary Hover**     | `#0056b3`               | Состояние при наведении для кнопок      |
| **Foreground (Text)** | `#0f172a`               | Основной текст (Slate 900)              |
| **Muted Text**        | `#64748b`               | Вспомогательный текст (Slate 500)       |
| **Border**            | `#e2e8f0`               | Светлые границы (Slate 200)             |
| **Glass**             | `rgba(255,255,255,0.7)` | База для эффекта матового стекла        |

---

## 🖋️ Типографика (Typography)

Система шрифтов построена на контрасте между мощными заголовками и функциональным основным текстом.

- **Заголовки (Headings):** `Montserrat`, sans-serif.
  - _Стиль:_ Bold / ExtraBold (700-800).
  - _Letter Spacing:_ `-0.02em` (для плотности и премиальности).
- **Основной текст (Body):** `Inter`, system-ui.
  - _Стиль:_ Medium (500) для интерфейса, Regular (400) для лонгридов.
  - _Smoothing:_ `-webkit-font-smoothing: antialiased`.

---

## 🧊 Спецэффекты и Глубина

### Glassmorphism

Для хедера и некоторых карточек используется эффект матового стекла:

- `backdrop-filter: blur(12px)`
- `border: 1px solid rgba(255, 255, 255, 0.3)`
- `box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1)`

### Скругление (Border Radius)

Мы используем экстремально большие радиусы для создания мягкого, "дружелюбного" интерфейса:

- **Кнопки:** `12px`
- **Карточки места:** `2.5rem` (`40px`)
- **Контентные блоки:** `3rem` (`48px`)
- **Балуны карты:** `24px`

---

## 🧩 Компоненты

### 1. Интерактивные карточки (Location Cards)

- **Ховер-эффект:** `translate-y-[-8px]`, усиление тени, появление градиентного оверлея.
- **Изображение:** Легкий grayscale (0.2), переходящий в полный цвет при наведении. Масштабирование `scale-105`.

### 2. Кнопка Primary

- Скругление: `12px`.
- Тень: Акцентная синяя тень при ховере (`0 10px 20px -5px rgba(0, 112, 243, 0.3)`).
- Анимация: Подъем на 2px при наведении.

### 3. Балуны Яндекс.Карт (Custom Balloons)

Полностью кастомизированные элементы карты:

- **Close Button:** Минималистичный круглый крестик с тенью.
- **Layout:** Фото сверху (высота 110px), текст и кнопка действия снизу.
- **Transitions:** Внутренний зум фото при наведении на весь балун.

---

## ✨ Принципы взаимодействия (Interactivity)

1.  **Плавность:** Все изменения состояний (hover, focus) длятся `0.3s - 0.5s` с использованием `cubic-bezier`.
2.  **Обратная связь:** Интерактивные элементы должны визуально реагировать на пользователя (смещение, изменение тени, появление стрелок).
3.  **Иерархия:** Самый важный контент всегда выделяется `font-extrabold` и `tracking-tight`.

---

## 🛠️ Стек реализации

- **Framework:** Next.js (App Router)
- **Engine:** PayloadCMS 3.0
- **Styling:** Tailwind CSS + Vanilla CSS (для сложных переопределений Yandex Maps)
- **Icons:** Lucide React (или SVG инлайн)
